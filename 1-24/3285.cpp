#include<bits/stdc++.h>
#define For(i, l, r) for(int i = l; i <= r; i++)
#define Down(i, r, l) for(int i = r; i >= l; i--)

using namespace std;

int main(){
    int n, m;
    scanf("%d%d", &n, &m);                                 
    return 0;
}